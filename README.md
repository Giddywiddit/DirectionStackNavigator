# 🧭 Direction Stack Navigator (Java)

A console-based Java program that simulates navigation using stack operations
  
It reads a sequence of directions from the user, checks if they form a palindrome,  
and prints the reverse directions to return home

---

## 🚀 How to Run

### Compile:
```bash
javac DirectionStackNavigator.java
